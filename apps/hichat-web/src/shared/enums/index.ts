// Components

export enum SizeVariant {
  Large = 'large',
  Default = 'default',
  Small = 'small',
}
// Create a type like this  ButtonType = 'default' | 'error' | 'primary' | 'secondary' | 'success' | 'warning'; from enum ButtonType
// Not declared new type but get from enum
// Not the key only the value of the key

export enum ButtonType {
  Default = 'default',
  Error = 'error',
  Primary = 'primary',
  Secondary = 'secondary',
  Success = 'success',
  Warning = 'warning',
}
